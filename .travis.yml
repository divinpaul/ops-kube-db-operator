language: go

addons:
  apt:
    packages:
      - docker-ce

services:
  - docker

before_install:
  - make clean

script:
  - make ci

after_success:
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
    make login publish;
    fi
